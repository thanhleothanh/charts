# AWS EKS - staging namespace

namespace:
  name: staging
  createResourceQuota: true
  resourceQuota:
    requests:
      cpu: "2"
      memory: 4Gi
    limits:
      cpu: "4"
      memory: 8Gi

apps:
  kotlin-spring:
    enabled: true
    image:
      repository: ghcr.io/thanhleothanh/kotlin-spring
      tag: v1.0.0
    replicaCount: 2
    service:
      port: 8080
    ingress:
      enabled: true
      hosts:
        - host: kotlin-spring.staging.eks.example.com
          paths:
            - path: /
              pathType: Prefix
